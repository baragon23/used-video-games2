import { Box } from '@mui/material';
import { styled } from '@mui/material/styles';

const TitleShadow = styled(Box)(() => ({
	textShadow: '3px 2px 0 #585d5d, 6px 4px 0 rgba(0, 0, 0, 0.15)',
}));

export default TitleShadow;
